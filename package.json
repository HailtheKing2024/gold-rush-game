{
  "name": "gold-rush-game",
  "version": "0.1.0",
  "type": "module",
  "description": "Gold Rush game with global leaderboard on Render",
  "scripts": {
    "dev": "npx convex dev",
    "start": "serve . -l ${PORT:-3000}",
    "build": "npx convex codegen",
    "lint": "eslint .",
    "test": "jest",
    "posttest": "echo 'Pre-flight successful! Ready to ship to Render.'"
  },
  "dependencies": {
    "convex": "^1.34.0",
    "serve": "^14.2.6"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.0.3",
    "jest": "^30.3.0"
  }
}
